type GameMode = "ai" | "solo" | "multiplayer";

const useBoard = (): void => {
  const boardHistory = [];

  const restart = (): void => {
    //
  };

  const startGame = (gameMode: GameMode): void => {
    //
  };
};

export default useBoard;
